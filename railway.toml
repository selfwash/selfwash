# Railway: connect this GitHub repo, add PostgreSQL, set variables (see .env.example).
# https://docs.railway.com/

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python -u consumer.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 50
